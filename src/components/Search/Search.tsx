import React from "react";
import Presenter from "./Presenter";

const Search = () => {
  const temp = () => {};
  return <Presenter onChangeValue={temp} onSearch={temp} />;
};

export default Search;
